const { MatchLinker } = require("./match_linker_model")
const { Match, Pitfall } = require("./match_model")
const { User } = require("./user_model")

module.exports = {
    Pitfall,
    Match,
    User,
    MatchLinker
}
